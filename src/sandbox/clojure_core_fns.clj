(ns sandbox.clojure-core-fns)

(println "...")


;; vectors
[1 2 3 :a "asdf" [:a 1]]
;; sets
#{1 2 3}
;; maps
{:key "value"}
;; lists
'(1 2 3 2 1)
;; sequences
(seq [1 2 3])
;; empty?
(empty? #{})
;; get
(get [1 2 3] 0)
(get {:first "Darlei" :second "Soares" "qqcoisa" "ttl"} "qqcoisa")
;; get in -> get into nested maps and vectors
;; nth
;; assoc
;; assoc-in
;; dissoc
;; update
;; update-in
;; merge
;; merge with
;; count
;; sum
;; apply
;; first
;; second
;; nth
;; rest
;; last
;; but last



(defn qqcoisa
  []
  (println "Ola"))




